{
  "name": "devguard-helm-chart-schema",
  "private": true,
  "type": "module",
  "description": "Source of truth for the DevGuard Helm chart values.yaml and the Rancher questions.yaml. Edit schema.ts, then run `bun run generate`.",
  "scripts": {
    "generate": "bun run generate.ts",
    "check": "bun run generate.ts --check"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
