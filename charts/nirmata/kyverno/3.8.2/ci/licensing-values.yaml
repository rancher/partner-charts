# ct-lint case for a non-default licensing cadence. No `enabled` key on purpose:
# licensing is always on. The controller clamps the interval to 1m..1h.
features:
  licensing:
    evaluationInterval: 15m
