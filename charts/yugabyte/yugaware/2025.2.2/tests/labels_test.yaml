suite: Common labels are applied
templates:
  - "templates/**/*.yaml"   # glob all templates; adjust as needed
set:
  commonLabels:
    team: platform
tests:
  - it: every manifest has the common labels
    asserts:
      - isSubset:
          path: metadata.labels
          content:
            team: platform
