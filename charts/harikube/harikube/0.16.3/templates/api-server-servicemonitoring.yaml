{{- if .Values.apiServer.create -}}
{{- if .Values.apiServer.monitoring.create -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    {{- include "harikube.labels" . | nindent 4 }}
  name: harikube-api-server-sm
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:
  - bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    path: /metrics
    port: metrics
    scheme: https
    tlsConfig:
      ca:
        secret:
          key: ca.crt
          name: harikube-api-server-crt
      cert:
        secret:
          key: tls.crt
          name: harikube-api-server-crt
      insecureSkipVerify: false
      keySecret:
        key: tls.key
        name: harikube-api-server-crt
      serverName: harikube-api-server-svc.{{ .Release.Namespace }}.svc
  selector:
    matchLabels:
    {{- with .Values.apiServer.podLabels }}
    {{- toYaml . | nindent 6 }}
    {{- end }}
{{- end }}
{{- end }}
