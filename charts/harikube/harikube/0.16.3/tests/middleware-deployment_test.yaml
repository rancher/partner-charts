suite: test middleware deployment rendering
templates:
  - templates/middleware-deployment.yaml
tests:
- it: should render correct replica count
  asserts:
  - equal:
      path: spec.replicas
      value: 1
