apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    app.kubernetes.io/name: airlock-microgateway-operator
    app.kubernetes.io/version: 4.8.1
  name: graphqls.microgateway.airlock.com
spec:
  group: microgateway.airlock.com
  names:
    categories:
      - airlock-microgateway
    kind: GraphQL
    listKind: GraphQLList
    plural: graphqls
    singular: graphql
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: GraphQL defines a policy for enforcing a GraphQL schema.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired GraphQL schema enforcement configuration.
              properties:
                settings:
                  description: settings defines the settings to configure GraphQL.
                  properties:
                    allowIntrospection:
                      default: true
                      description: allowIntrospection specifies if the introspection system is exposed.
                      type: boolean
                    allowMutations:
                      default: true
                      description: allowMutations specifies if mutations are allowed.
                      type: boolean
                    schema:
                      description: schema specifies the GraphQL schema.
                      properties:
                        source:
                          description: source specifies the GraphQL (October 2021 Edition) schema to be enforced.
                          properties:
                            configMapRef:
                              description: |-
                                configMapRef references the configmap by its name containing the well-known key `schema.graphql`. Use the `data` field for plaintext or the `binaryData` for compressed data. Supported compression formats are zstd, gzip or zip.
                                Note: If the provided GraphQL schema is invalid (e.g., due to syntax errors), the Airlock Microgateway Engine will log an error, GraphQL validation will always fail, and requests will be blocked.
                              properties:
                                name:
                                  description: name of the resource
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              required:
                                - name
                              type: object
                          type: object
                      required:
                        - source
                      type: object
                    threatHandlingMode:
                      default: Block
                      description: threatHandlingMode specifies how threats should be handled.
                      enum:
                        - Block
                        - LogOnly
                      type: string
                  type: object
              type: object
          type: object
      served: true
      storage: true
