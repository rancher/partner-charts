# Default values for nutanix-csi-snapshot.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Global Settings for all pods

nodeSelector: {}
tolerations: []
imagePullPolicy: IfNotPresent

## If you have any image pull secrets you can add it here
imagePullSecrets:
#  - name: regcred

controller:
  replicas: 2
  image: registry.k8s.io/sig-storage/snapshot-controller
  # Image tag for snapshot controller (supports k8s >= 1.25)
  tag: v8.3.0
  nodeSelector: {}
  tolerations: []

singleNodeCluster: false
