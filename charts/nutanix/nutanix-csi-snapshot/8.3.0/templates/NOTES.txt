Snapshot controller is deployed in the cluster in {{ .Release.Namespace }} namespace.

Version {{ .Values.controller.tag }} was deployed

✅ Verify installation:
   kubectl get pods -n {{ .Release.Namespace }} -l app=csi-snapshot-controller
   kubectl get crd | grep snapshot
