{{- if .Values.global.federatedStorage.config -}}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "kubecost.federatedStorage.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-analyzer.commonLabels" . | nindent 4 }}
data:
  federated-store.yaml: {{ .Values.global.federatedStorage.config | b64enc }}
{{- end }}
