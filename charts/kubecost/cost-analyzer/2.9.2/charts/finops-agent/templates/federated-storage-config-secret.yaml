{{- /*
Copyright IBM, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and (.Values.global.federatedStorage.config) (not (eq .Values.global.chartName "kubecost")) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-federated-storage-config
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" .) | nindent 4 }}
    app.kubernetes.io/part-of: finops-agent
    app.kubernetes.io/component: config
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" .) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- include "finops-agent.federatedStorageFileName" . | nindent 2}}: {{ .Values.global.federatedStorage.config | b64enc | quote }}
{{- end }}
