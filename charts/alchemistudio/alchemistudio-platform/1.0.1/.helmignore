# Ship the two CUSTOMER-facing docs (Helm's .helmignore has no "!" negation, so we
# can't do "*.md + !..."; instead we exclude the maintainer-only docs by name):
#   README.md      — product description (chart-detail page)
#   app-readme.md  — Rancher "before install" description (timeout + guardrails guidance)
# Exclude the maintainer/internal docs:
DESIGN.md
PATCHED-SUBCHARTS.md
.git

# publish-script token-bake backups — never package these
*.prebake
*.bak
