{{- if .Values.global.dex.enabled -}}

{{- if .Values.dex.manageConfigSecret -}}
{{- $dexConfigSecretName := .Values.dex.configSecret.name | default "dex-config" -}}
{{- $dexSecret := (lookup "v1" "Secret" .Release.Namespace $dexConfigSecretName).data | default dict -}}
{{- $prevUiClientSecret := (get $dexSecret "uiClientSecret") -}}
{{- $uiClientSecret := empty $prevUiClientSecret | ternary (randAlphaNum 32) (b64dec $prevUiClientSecret) -}}
{{- $rancherUrl := .Values.global.cattle.url | default .Values.rancher.url -}}

---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  annotations:
  name: {{ $dexConfigSecretName }}
  namespace: {{ .Release.Namespace }}
stringData:
  issuer: {{ include "epinio-dex-issuer" . }}
  endpoint: {{ printf "http://%s.%s.svc.cluster.local:5556" .Values.dex.fullnameOverride .Release.Namespace }}
  uiClientSecret: {{ $uiClientSecret }}
  config.yaml: |-
{{- if $rancherUrl }}
    web:
      allowedOrigins: ['{{ $rancherUrl }}']
{{- end }}
    issuer: {{ include "epinio-dex-issuer" . }}
    storage:
      type: kubernetes
      config:
        inCluster: true
    oauth2:
      skipApprovalScreen: true
    enablePasswordDB: true
    staticPasswords:
    - email: "admin@epinio.io"
      # bcrypt hash of the string "password": $(echo password | htpasswd -BinC 10 admin | cut -d: -f2)
      hash: "$2a$10$2b2cU8CPhOTaGrs1HRQuAueS7JTT5ZHsHSzYiFPm1leZck7Mc8T4W"
      username: "admin"
      userID: "08a8684b-db88-4b73-90a9-3cd1661f5466"
    - email: "epinio@epinio.io"
      hash: "$2a$10$2b2cU8CPhOTaGrs1HRQuAueS7JTT5ZHsHSzYiFPm1leZck7Mc8T4W"
      userID: "08a8684b-db88-4b73-90a9-3cd1661f5467"

    staticClients:
    - id: epinio-api
      name: 'Epinio API'
      public: true
      # The 'Epinio API' lets the 'Epinio cli' issue ID tokens on its behalf.
      # https://dexidp.io/docs/custom-scopes-claims-clients/#cross-client-trust-and-authorized-party
      trustedPeers:
      - epinio-cli
      - epinio-ui
{{- if $rancherUrl }}
      - rancher-dashboard
{{- end }}

    - id: epinio-cli
      name: 'Epinio cli'
      public: true

    - id: epinio-ui
      name: 'Epinio UI'
      secret: {{ $uiClientSecret | quote }}
      # Shouldn't be public, https://dexidp.io/docs/custom-scopes-claims-clients/#public-clients
      redirectURIs:
      - {{ .Values.dex.ui.redirectURI | default (printf "https://%s/auth/verify/" (include "epinio-hostname" .)) | quote }}

{{- if $rancherUrl }}
    - id: rancher-dashboard
      name: 'Rancher Dashboard'
      public: true
      redirectURIs:
      - "{{ $rancherUrl }}/dashboard/epinio/auth/verify/"
{{- end }}
{{- end }}
---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "epinio-truncate" (print "user-" "admin@epinio.io") }}
  namespace: {{ .Release.Namespace }}
  labels:
    epinio.io/api-user-credentials: "true"
  annotations:
    epinio.io/roles: "admin"
stringData:
  username: "admin@epinio.io"
  password: "$2a$10$2b2cU8CPhOTaGrs1HRQuAueS7JTT5ZHsHSzYiFPm1leZck7Mc8T4W"

---
# TODO (Future):  Remove this custom resource and leverage Dex Helm chart values instead
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dex
  namespace: {{ .Release.Namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ default .Values.global.tlsIssuer .Values.global.customTlsIssuer | quote }}
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key | quote }}: {{ $value | quote }}
    {{- end }}
spec:
  {{- if .Values.ingress.ingressClassName }}
  ingressClassName: "{{ .Values.ingress.ingressClassName }}"
  {{- end }}
  rules:
  - host: {{ (include "epinio-dex-hostname" .) | quote }}
    http:
      paths:
      - backend:
          service:
            name: dex
            port:
              number: {{ .Values.dex.service.ports.http.port | default 5556 | int }}
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - {{ (include "epinio-dex-hostname" .) | quote }}
    secretName: dex-tls

{{- end }}
