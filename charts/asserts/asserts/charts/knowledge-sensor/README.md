# Asserts Knowledge Sensor Helm Chart

Installs Asserts Knowledge Sensor for federation from a customer Prometheus server
and remote-writes to the Asserts Cloud.

## Requirements

* `helm` version >= 3.0 (`brew install helm`)

## Deploying Charts

TODO
