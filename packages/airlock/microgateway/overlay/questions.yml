questions:
  - variable: operator.gatewayAPI.enabled
    type: boolean
    required: true
    description: "Enable Gateway API Support. Requires that the gateway.networking.k8s.io/v1 resources are installed on the cluster."
    label: "Enable (IMPORTANT: Must be enabled!)"
    group: "Gateway API Support"
